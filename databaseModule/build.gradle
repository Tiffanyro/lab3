plugins {
    id 'java'
    id 'application'
}

mainClassName = 'Main' // Update with your main class name

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.xerial.sqlite-jdbc:sqlite-jdbc:3.45.1' // Adjust the version as needed
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}